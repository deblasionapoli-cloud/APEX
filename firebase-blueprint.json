{
  "entities": {
    "JournalEntry": {
      "title": "Journal Entry",
      "description": "A single piece of remembered information or interaction context.",
      "type": "object",
      "properties": {
        "content": {
          "type": "string",
          "description": "The textual content of the memory."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the memory was created."
        },
        "category": {
          "type": "string",
          "description": "The type of memory (e.g., 'fact', 'preference', 'emotion')."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user this memory belongs to."
        }
      },
      "required": ["content", "timestamp", "userId"]
    },
    "RemoteCommand": {
      "title": "Remote Command",
      "description": "A command sent from a remote device to be processed by the main display.",
      "type": "object",
      "properties": {
        "command": {
          "type": "string"
        },
        "timestamp": {
          "type": "string",
          "format": "date-time"
        },
        "processed": {
          "type": "boolean"
        },
        "userId": {
          "type": "string"
        }
      },
      "required": ["command", "timestamp", "userId"]
    }
  },
  "firestore": {
    "/memories/{memoryId}": {
      "schema": "JournalEntry",
      "description": "A collection of long-term memories for GLITCH.CORE."
    },
    "/remote_inputs/{inputId}": {
      "schema": "RemoteCommand",
      "description": "Remote commands for terminal input synchronization."
    }
  }
}
